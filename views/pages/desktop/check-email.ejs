<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>BioBrain - Verify Email</title>
  <link rel="stylesheet" href="/css/desktopStyle.css" />
</head>

<body class="view-pages-desktop-check-email">
  <%- include('../../partials/desktop/header') %>

  <div class="login-container">
    <h2>Verify Your Email</h2>
    
    <div class="desktop-inline-11zvx75">
      <p class="desktop-inline-pesw2g">
        We've sent a verification link to:<br>
        <strong class="desktop-inline-10aefdw"><%= email %></strong>
      </p>
      <p class="desktop-inline-10l7wx6">
        Please click the link in the email to activate your account.
      </p>
    </div>

    <div class="desktop-inline-cnrfi0" id="resendSection">
      <p class="desktop-inline-h2au7e">Didn't receive the email?</p>
      <button class="desktop-inline-xk9cqr" id="resendBtn"  >
        Resend Verification Email
      </button>
    </div>

    <div class="links desktop-inline-xk9zqa">
      <a href="/authentication/login">Back to Login</a>
    </div>

    <div class="error desktop-inline-xn3h6r" id="statusMsg"></div>
  </div>

  <script>
    document.getElementById("resendBtn").addEventListener("click", async () => {
      const statusMsg = document.getElementById("statusMsg");
      const resendBtn = document.getElementById("resendBtn");

      // Reset styles
      statusMsg.innerText = "Sending...";
      statusMsg.style.color = "#64748b"; 
      resendBtn.disabled = true;
      resendBtn.style.opacity = "0.5";

      try {
        const res = await fetch("/authentication/resend-verification", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ email: "<%= email %>" })
        });

        const data = await res.json();
        
        statusMsg.innerText = data.message;
        statusMsg.style.color = data.success ? "#10b981" : "#ef4444"; // Green for success, red for error

        // If successful, prevent spamming by keeping button disabled for 30 seconds
        if (data.success) {
          setTimeout(() => {
            resendBtn.disabled = false;
            resendBtn.style.opacity = "1";
          }, 30000);
        } else {
          resendBtn.disabled = false;
          resendBtn.style.opacity = "1";
        }

      } catch (e) {
        statusMsg.innerText = "Server error! Please try again later.";
        statusMsg.style.color = "#ef4444";
        resendBtn.disabled = false;
        resendBtn.style.opacity = "1";
      }
    });
  </script>

  <%- include('../../partials/desktop/footer') %>
</body>

</html>